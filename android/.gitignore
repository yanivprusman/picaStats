*.iml
.gradle
.kotlin
/local.properties
/.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Bind-mounted feedback library
feedback-lib/

# Local build config (holds STATS_TOKEN — never commit; repo is public)
.env
